name: Build SolidWorks Add-in

on:
  push:
    paths:
      - 'solidworks-addin/**'
      - '.github/workflows/build-addin.yml'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: windows-2022

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Publish add-in
        run: dotnet publish solidworks-addin/TrentCAD.SolidWorksAddin/TrentCAD.SolidWorksAddin.csproj -c Release -o solidworks-addin/publish

      - name: Upload add-in artifact
        uses: actions/upload-artifact@v4
        with:
          name: TrentCAD-SolidWorksAddin
          path: solidworks-addin/publish/
          retention-days: 90
